#include "pch.h"
#include "infection.h"


int main()
{
    DISK_GEOMETRY pdg;
    bool bResult;                 // generic results flag
    bResult = GetDriveMsg(&pdg, 0);
    bool res = GetPEMsg();
}
